<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ulvia.katalogbaju">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_jilbrave_round"
        android:label="Jilbrave Fashion"
        android:roundIcon="@mipmap/logo_jilbrave_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShoesActivity"></activity>
        <activity android:name=".BagsActivity" />
        <activity android:name=".model.Item" />
        <activity android:name=".model.Image" />
        <activity android:name=".DressActivity" />
        <activity android:name=".RecycleActivity" />
        <activity android:name=".adapters.JilbraveAdapter" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>